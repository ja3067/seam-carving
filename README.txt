Image carving script in Python and C++

Run with ./carve [path to image file]. C++ version has a bug in copyTo for small images that I can't track down.

Inspired by an early CV image carving paper, simple DP shortest path algorithm using gradient weights.
